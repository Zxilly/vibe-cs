# 信息架构重构 · 执行任务清单

> 源文档:`2026-08-19-ia-restructure-prd.md`(问题定义、用户故事编号、模块决策 D1–D7 以此为准)。
> 执行原则:每个任务是纵穿所有层的 tracer bullet,独立完成、独立可验证;完成一个关一个,不顺手做下一个任务的事。

## 纪律(每个任务的完成门,无一例外)

1. `pnpm lint && pnpm typecheck && pnpm test` 全绿才允许勾掉任务(重设计 spec §9 风险 2 的既定纪律)。
2. 保留清单(PRD Further Notes)对应的现有测试只许改断言位置,不许删除。
3. 术语层只管 UI 文案:数据模型、IPC、存储键名、测试 fixtures 的英文标识符不改。
4. 设计系统不动:token、primitives、layout、Blueprint 不新增不改写(PRD D1)。
5. 每任务结束跑一遍浏览器目视检查(agent-browser),截图留证。

## 状态图例

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 完成,验收标准逐条通过
- `[!]` 受阻,原因记录在任务下方

## 依赖关系一图

```
IA-01 ──┬── IA-02 ──► IA-03 ──┐
        ├── IA-04            │
        ├── IA-05 ──► IA-06 ──► IA-07 ──┬── IA-08 ──► IA-11 ─┐
        │                             ├── IA-09            │
        │                             ├── IA-10            ├──► IA-14
        │                             └── IA-12            │
        └── IA-13 ─────────────────────────────────────────┘
```

---

## 阶段 1 —— 术语与壳层(页面内部不动)

### IA-01 术语映射表 + chrome 层文案替换

- [x] **状态**
- 阻塞于:无 —— 可立即开始
- 用户故事:44, 45
- 建什么:建立单一术语映射表(方案→剪辑单、变更→修改、会话→对话、镜头→片段、合辑/工程→作品、输出→成品文件、任务记录→后台任务),替换壳层(导航、标题栏、面包屑、空态、按钮)文案。页面内部的深度文案留到对应迁入任务。
- 验收标准:
  - [x] 映射表只有一个出处,任何页面不得自行另写同义词
  - [x] 壳层文案(导航项、Toolbar 标题、面包屑)全部使用新术语
  - [x] 新增快照断言:旧术语不出现在 chrome 层文案
  - [x] `lingui extract + compile` 后双语言 locale 全绿,CI 的 `git diff --exit-code src/locales` 门通过
  - [x] 目视检查:六个页面在 1100×700 视口通过,截图见 `docs/superpowers/evidence/2026-08-20-ia-01/`

### IA-02 活动抽屉 + 任务记录退出导航

- [x] **状态**
- 阻塞于:无(IA-01 已完成)
- 用户故事:7, 42, 43
- 建什么:标题栏增加铃铛入口(未读计数),点击打开活动抽屉:分状态任务列表,条目打开现有任务详情(覆盖层呈现)。交付页摘掉任务记录 Seg,`/delivery?view=tasks` redirect 到 `/delivery` 并自动打开抽屉。导航摘除任务记录项。
- 验收标准:
  - [x] 抽屉列出分析/下载/录制/导出四类任务,含状态、进度、失败原因
  - [x] 失败任务在抽屉内有重试入口
  - [x] 条目详情与现任务详情页信息一致,不缩水
  - [x] `/delivery?view=tasks` 旧链接落到成品页且抽屉自动打开
  - [x] 未读计数在有新任务状态变化时更新
  - [x] 抽屉有可访问名称与键盘关闭(Esc),延续现有 Dialog 约定
  - [x] 交互测试:未读计数、打开详情、redirect 行为;1100×700 截图见 `docs/superpowers/evidence/2026-08-20-ia-02/`

### IA-03 首页三区块重构

- [x] **状态**
- 阻塞于:无(IA-01、IA-02 已完成)
- 用户故事:1, 2, 3, 4, 5, 6, 8
- 建什么:首页改为三区块——「需要我处理」(待确认剪辑单、可重试失败、环境问题,各带原地动作)→「继续」(最近作品卡片,带步骤状态)→「新建」(新建作品 + 导入素材入口)。任务进度墙下线(已入抽屉)。FirstRunStrip 改写为新建区块的空态引导。
- 验收标准:
  - [x] 三区块按此顺序渲染,骨架测试断言存在与顺序
  - [x] 待办条目各带原地动作(审阅/重试/去设置),不跳走
  - [x] 首屏无任务 UUID 原文;任务一律以可读名称出现
  - [x] 素材为空的首次使用态指向导入 Demo,单一引导
  - [x] 环境提示(NVENC 等)是"需要我处理"的一等条目
  - [x] 数据源全部复用现有 query 层,无新增 IPC
  - [x] 目视检查:1100×700 折叠态三区块不错乱,截图见 `docs/superpowers/evidence/2026-08-20-ia-03/`

### IA-04 Agent 入口收敛(右缘竖条移除)

- [x] **状态**
- 阻塞于:无(IA-01 已完成)
- 用户故事:36, 37
- 建什么:移除右缘「AI 工作台」竖条及其挂载逻辑;各页面「用 Agent 制作视频」主按钮统一改为「新建作品」,行为暂保持跳 `/agent`(IA-08 迁入后改指作品工作区)。Ctrl K 的 Agent 命令保留。
- 验收标准:
  - [x] 右缘竖条在所有页面不可见,壳层无残留挂载点
  - [x] 全应用无「用 Agent 制作视频」文案;主按钮统一为「新建作品」
  - [x] Ctrl K 命令面板中 Agent 相关命令仍可达
  - [x] 三表绑定测试(路由/导航/命令)更新后全绿
  - [x] 目视检查:首页、资料库、比赛工作区通过,截图见 `docs/superpowers/evidence/2026-08-20-ia-04/`

---

## 阶段 2 —— 作品对象与工作区骨架

### IA-05 作品聚合只读视图模型

- [x] **状态**
- 阻塞于:无(IA-01 已完成)
- 用户故事:20, 21, 35
- 建什么:domain/data 层新增「作品」只读聚合:从现有 agent plan、montage 工程、editor 工程、录制任务、输出文件的 query 结果拼合作品视图模型(关联比赛、剪辑单引用、剪辑模式、录制任务、成品文件、当前步骤推断)。纯前端拼装,不新增 IPC;现存工程首读 1:1 包裹为作品。
- 验收标准:
  - [x] 每个现存 plan/montage 工程/editor 工程都能映射到一个作品,无遗漏
  - [x] 作品的当前步骤推断规则(有无可编辑剪辑单/有待录制片段/有成品)有单测覆盖
  - [x] 无对应实体的空作品与无作品的孤儿实体的行为有明确定义并测试
  - [x] 聚合失败(某一来源查询失败)降级为部分作品卡片而非整页报错
  - [x] 单测只测视图模型外部行为,不测拼装细节

### IA-06 作品列表页 `/projects`

- [x] **状态**
- 阻塞于:无(IA-05 已完成)
- 用户故事:19, 35
- 建什么:新路由 `/projects` 渲染作品列表(名称、关联比赛、当前步骤、状态、最近活动),空态引导新建。导航「作品」项上线。路由/导航/命令三表同步注册。
- 验收标准:
  - [x] 列表卡片显示名称、步骤、状态、最近活动时间
  - [x] 空态指向「新建作品」单一动作
  - [x] 点击卡片进入 `/projects/:projectId`
  - [x] 三表绑定测试含新条目,`matchRoutes` 全绿
  - [x] 骨架测试:列表、空态、加载态

### IA-07 作品工作区骨架 `/projects/:projectId`

- [x] **状态**
- 阻塞于:无(IA-06 已完成)
- 用户故事:21, 22, 46
- 建什么:作品工作区骨架:顶部步骤条(选材 → 剪辑单 → 录制 → 导出,`?step=` 切换),步骤门控状态机(无片段不能录制、未确认剪辑单不能导出),门控原因一句话可见;四步先放占位视图(标明迁入来源)。旧路由 redirect 表落地:`/agent`、`/recording`、`/montage/:id`、`/editor/:id` 指向对应工作区步骤。
- 验收标准:
  - [x] 步骤条可任意回退,不锁定顺序(导航元件,非向导)
  - [x] 不可达步骤显示一句话原因(延续 `disabledReason` 约定)
  - [x] 门控状态机单测覆盖全部合法/非法转移
  - [x] 旧路由 redirect 逐条断言
  - [x] `?step=` 非法值回落到第一个可达步骤
  - [x] 骨架测试:步骤条、门控文案、四步占位
  - [x] 目视检查:1100×700 折叠态步骤条可用,截图见 `docs/superpowers/evidence/2026-08-20-ia-07/`

---

## 阶段 3 —— 能力迁入工作区

### IA-08 剪辑单步:Agent 面板迁入

- [x] **状态**
- 阻塞于:无(IA-07 已完成)
- 用户故事:26, 27, 28, 29, 36, 39
- 建什么:现有 Agent 创作页的能力(对话、剪辑单生成、逐条接受/拒绝的修改、就地编辑、候选片段、会话历史与修订记录)整体迁入工作区「剪辑单」步,以作品为上下文。`/agent` 路由退役,redirect 生效。IA-04 的「新建作品」按钮改指工作区剪辑单步。
- 验收标准:
  - [x] 剪辑单步内 Agent 对话可读作品上下文(片段编号可指代)
  - [x] 每轮修改可逐条接受/拒绝(现有变更模型不变)
  - [x] 手动编辑不打断 Agent、不需批准(现状保留)
  - [x] 会话历史与修订记录随作品可查
  - [x] 「新建作品」主按钮全应用指向工作区
  - [x] `/agent` 下无功能残留,redirect 断言通过;截图见 `docs/superpowers/evidence/2026-08-20-ia-08/`

### IA-09 剪辑单步:快速合辑迁入

- [x] **状态**
- 阻塞于:无(IA-07 已完成)
- 用户故事:26, 34
- 建什么:快速合辑(片段排序、音乐/节拍、导出设置)作为剪辑单步的「快速模式」迁入;montage 工程以作品剪辑模式呈现,可继续编辑。`/montage` 退役,redirect。
- 验收标准:
  - [x] 现存合辑工程在作品内打开并可继续编辑
  - [x] 快速模式与 Agent 模式在剪辑单步内切换不丢未保存修改(快速模式即时写回,跨模式无转换契约时明确禁用说明)
  - [x] 合辑导出走现有契约不变
  - [x] `/montage/:id` 旧链接落到对应作品剪辑单步;截图见 `docs/superpowers/evidence/2026-08-20-ia-09/`

### IA-10 剪辑单步:多轨编辑器迁入

- [x] **状态**
- 阻塞于:无(IA-07 已完成)
- 用户故事:30, 34
- 建什么:多轨编辑器(轨道、入点出点、预览、媒体库)作为剪辑单步的「精剪模式」迁入;editor 工程同上包裹。`/editor` 退役,redirect。
- 验收标准:
  - [x] 现存多轨工程在作品内打开并可继续编辑
  - [x] 编辑器媒体库可引用作品关联比赛的素材
  - [x] 精剪模式与快速/Agent 模式切换语义明确(哪些修改双向可见,文档+测试)
  - [x] `/editor/:id` 旧链接落到对应作品剪辑单步;截图见 `docs/superpowers/evidence/2026-08-20-ia-10/`

### IA-11 录制步:录制队列迁入

- [x] **状态**
- 阻塞于:无(IA-07、IA-08 已完成)
- 用户故事:31, 32
- 建什么:「录制」步列出本作品的录制队列、每片段进度、取消;录制环境未就绪时给出原因与设置入口(现有降级规则)。可录制方案列表(原录制计划页左列)由作品剪辑单步的"送去录制"动作取代。`/recording` 退役,redirect。
- 验收标准:
  - [x] 录制步队列与现有录制管线契约一致,可取消
  - [x] 环境未就绪(编码器/HLAE)时按钮禁用并写明原因
  - [x] 录制进度同时出现在活动抽屉(单一数据源)
  - [x] `/recording` 下无功能残留,redirect 断言通过;截图见 `docs/superpowers/evidence/2026-08-20-ia-11/`

### IA-12 选材步 + 比赛工作区「加入作品」

- [x] **状态**
- 阻塞于:无(IA-07 已完成)
- 用户故事:23, 24
- 建什么:比赛工作区(含证据检索结果)的「加入视频」类动作统一改为「加入作品」(选择既有作品或新建);选材步汇总该作品关联的比赛与已收集片段。
- 验收标准:
  - [x] 高光、回合、证据条目均可"加入作品"
  - [x] 选材步按比赛分组展示已收集片段,可移除
  - [x] 加入动作后有明确的已加入反馈,指向作品
  - [x] 比赛工作区九视图行为不变(保留清单回归);截图见 `docs/superpowers/evidence/2026-08-20-ia-12/`

---

## 阶段 4 —— 合并与收尾

### IA-13 素材中心合并

- [x] **状态**
- 阻塞于:无(IA-01 已完成)
- 用户故事:9, 10, 11, 12, 13
- 建什么:`/history` 并入 `/library` 第二视图(Steam 下载),素材行级新增「新建作品(以此为起点)」动作。导航「素材」项落定。导入、监听目录、Steam 下载三入口同页。
- 验收标准:
  - [x] 资料库与 Steam 历史在同一页两个视图切换
  - [x] 素材行显示分析状态(未分析/分析中/已就绪/失败)
  - [x] 行级「新建作品」直接进入工作区并关联该比赛
  - [x] `/history` redirect 到 `/library` 对应视图
  - [x] 筛选、列配置、视图保存等现有能力回归;截图见 `docs/superpowers/evidence/2026-08-20-ia-13/`

### IA-14 旧代码删除 + 全量回归收口

- [x] **状态**
- 阻塞于:IA-08, IA-09, IA-10, IA-11, IA-12, IA-13
- 用户故事:18, 50
- 建什么:删除已退役页面与死代码(旧 Agent 页、录制计划页、独立合辑/编辑器列表页的残留);术语快照收口(页面内部深度文案);按 PRD 保留清单逐条全量回归。
- 验收标准:
  - [x] 退役路由对应源码删除,无死引用
  - [x] 保留清单逐条人工核对通过,记录在任务下方
  - [x] 旧术语快照断言覆盖页面内部,无漏网
  - [x] 全量测试、lint、typecheck 全绿;浏览器全页面目视复查一遍
  - [x] PRD 标注「已落地」并记录与 PRD 的偏离(若有)

#### PRD 保留清单核对

- [x] 比赛工作区九视图全部仍在 `MATCH_VIEWS` 穷举注册表中，目录级 43 文件/612 项回归与 IA-14 全页面截图通过。
- [x] 高光检测类型、逐条/批量「加入作品」及回合选材链路通过高光、回合和 IA-12 闭环测试。
- [x] 证据标签、跨回合集合、跨比赛检索及选手/地图/武器/事件筛选测试通过。
- [x] 玩家目录、玩家档案、近期比赛、趋势与热力图页面可达，详情页截图通过。
- [x] ARPL 回放、热力图维度、雷达占位与地图选择交互测试保留。
- [x] Demo 导入校验、监听目录、Steam 同步/下载、缺失文件状态与素材分析状态回归通过。
- [x] 饰品后端路由、客户端方法与生成 DTO 未被 IA 删除；基线无前端入口，已在 PRD 偏离中如实记录。
- [x] 录制预检、HLAE 环境门、启动互斥、逐片段阶段与取消回归通过。
- [x] 成品文件的受管/外部状态、可回滚删除和无效记录清理回归通过。
- [x] 恢复中心、设置五节、使用引导均完成 1100×700 目视检查。

---

## 阶段 5 —— 一级页面视觉框架收口

### IA-15 一级页面几何与控制层级统一

- [x] **状态**
- 阻塞于:无(IA-14 已完成)
- 用户故事:1, 9, 19, 35, 50
- 建什么:审计工作台、Demo 资料库、玩家目录、证据检索、作品、成品文件、设置与诊断七个一级入口，以现有 design tokens 和 layout primitives 收口页面几何。所有页面共享 56px 标题栏；数据筛选与折叠子导航使用 46px 次级控制带；常驻右侧上下文面板统一为 380px；移除重复标题和自由高度筛选区。保留各页面因任务类型不同而必要的表格、画布、设置分栏差异。
- 验收标准:
  - [x] 七个一级入口均保留固定 56px 页面标题栏，标题、摘要和主动作基线一致
  - [x] 玩家搜索迁入 46px 次级控制带，资料库与玩家目录的数据区起点一致
  - [x] 证据检索的搜索与条件固定为两条 46px 控制带，不再由内容自由撑高
  - [x] 资料库、玩家比较与证据详情的常驻上下文面板统一为 380px
  - [x] 折叠态 SubNav 使用 46px 次级控制带，设置页与数据页内容起点一致
  - [x] 成品页删除重复「成品文件」二级标题，仅保留筛选控件
  - [x] 1280×720 与 1100×700 截图复核通过，完整 lint、typecheck、test 全绿；证据见 `docs/superpowers/evidence/2026-08-20-ia-15/`

---

## 阶段 6 —— 剪辑 / 分析双模式

### IA-16 壳层模式切换

- [x] **状态**
- 阻塞于:无(IA-15 已完成)
- 建什么:左上角品牌块替换为「剪辑模式 / 分析模式」菜单，模式持久化；剪辑导航收敛到工作台、Demo 资料库、作品、成品文件，分析导航收敛到 Demo 资料库、玩家目录、证据检索。模式切换只改变入口视角，不复制数据。
- 验收标准:
  - [x] 模式按钮与侧栏共用 216/56px 几何，宽屏与折叠态均可操作
  - [x] 模式菜单使用受控 `DropdownMenuRadioGroup`，当前态与双向切换由 radio value 表达；截图见 `docs/superpowers/evidence/2026-08-20-ia-16/mode-menu-shadcn.png`
  - [x] 分析模式与剪辑模式各自只显示相关一级入口，设置始终在页脚
  - [x] 剪辑模式「Agent 创作」直达作品工作区 Agent 剪辑单，独立 `/agent` 页面与右缘栏继续保持退役
  - [x] 展开侧栏的设置激活面占满 footer 内容宽度，折叠按钮叠加而不分走一列
  - [x] 分析/剪辑专属深链自动选择正确模式，共享路由保留用户当前选择
  - [x] 模式选择写入 shell 持久状态，刷新后保留

### IA-17 初次打开引导

- [x] **状态**
- 阻塞于:IA-16
- 建什么:首次打开显示模式选择引导，解释两种模式共享数据、各自负责的工作，以及左上角可随时切换；选择或明确保持当前模式后不再自动出现。
- 验收标准:
  - [x] 两个模式选择均有可访问名称、说明和 `aria-pressed` 状态
  - [x] 选择分析模式落到 Demo 资料库，选择剪辑模式落到工作台
  - [x] Esc/取消使用当前模式并完成引导，不用误导性的「稍后」文案
  - [x] 交互截图见 `docs/superpowers/evidence/2026-08-20-ia-16/first-run.png`

### IA-18 分析能力保留契约

- [x] **状态**
- 阻塞于:IA-16
- 建什么:参考 `E:\Temp\cs-demo-manager` 的路由与能力面，建立当前 Vibe CS 分析能力映射；用跨层契约测试锁定分析导航、路由、Ctrl K 命令和单场九视图，明确未拥有契约的 Ban tracking / 语音导出不创建空入口。
- 验收标准:
  - [x] Demo/比赛、玩家、证据、Steam 下载均在分析模式或其直接下钻路径可达
  - [x] 单场比赛九视图完整保留，测试断言精确列表
  - [x] 分析任务继续使用全局活动抽屉，不因模式切换隐藏
  - [x] PRD 写明参考项目能力到当前实现的逐项映射与真实缺口
  - [x] 全量 lint、typecheck、357 个测试文件/4265 项测试与 Desktop 双模式目视回归通过；证据见 `docs/superpowers/evidence/2026-08-20-ia-16/`

---

## 执行日志

> 每完成一个任务,在此追加一行:日期、任务号、一句话结果、与 PRD 的偏离(无偏离写"无")。

- 2026-08-20, IA-01:建立单一 UI 术语映射,完成导航/面包屑/Toolbar/空态/动作的新术语替换;Lingui 1916/1916、lint、typecheck、4218 项测试及六页浏览器目视检查通过;无偏离。
- 2026-08-20, IA-02:标题栏活动铃铛、状态分组任务抽屉、失败重试、完整详情覆盖层与旧链接 redirect 落地;Lingui 1920/1920、lint、typecheck、4223 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-03:首页收敛为"需要我处理/继续/新建"三区块,下线任务进度墙与成品侧栏,补齐作品步骤状态和单一导入引导;Lingui 1922/1922、lint、typecheck、4224 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-04:移除 AppShell 的 Agent 右缘竖条挂载,全应用主 CTA 统一为"新建作品",保留侧栏与 Ctrl K Agent 入口;Lingui 1921/1921、lint、typecheck、4240 项测试及三页浏览器检查通过;无偏离。
- 2026-08-20, IA-05:新增纯前端作品聚合模型与 query 组合 hook,完成三类旧实体 1:1 包装、步骤推断、孤儿隔离和来源失败降级;lint、typecheck、4245 项测试及浏览器回归通过;无偏离。
- 2026-08-20, IA-06:新增 `/projects` 作品列表、导航与命令入口,卡片展示步骤/状态/比赛/最近活动并链接可达工作区地址;Lingui 1935/1935、lint、typecheck、4270 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-07:作品工作区步骤条、门控状态机、禁用原因、四步占位与 agent/recording/montage/editor 旧路由 redirect 落地;Lingui 1942/1942、lint、typecheck、4278 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-08:现有 Agent 对话/修改决策/手动编辑/候选片段/会话历史/修订记录整体嵌入作品剪辑单步骤,新建入口与 handoff 直达工作区;Lingui 1944/1944、lint、typecheck、4279 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-09:现有快速剪辑工作区整体嵌入作品剪辑单步骤,即时写回保证离开/返回不丢修改,导出契约不变,独立导航退役;Lingui 1951/1951、lint、typecheck、4279 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-10:现有多轨编辑器整体嵌入作品剪辑单步骤,合并项目与全局比赛素材,明确本地保存和无跨模式转换契约,独立导航退役;Lingui 1952/1952、lint、typecheck、4278 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-11:录制计划与任务队列迁入作品录制步,补齐逐片段进度、共享活动源取消、环境阻塞说明和设置入口,独立导航退役;Lingui 1969/1969、lint、typecheck、4278 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-12:比赛工作区高光/回合与跨比赛证据统一改为"加入作品",支持选择既有作品或新建,选材步按比赛汇总并可移除;按 D3 使用纯前端本地持久集合,未新增 IPC;Lingui 1982/1982、lint、typecheck、4282 项测试及 1100×700 浏览器闭环检查通过;无偏离。
- 2026-08-20, IA-13:Demo 资料库与 Steam 历史合并为素材中心双视图,退役独立历史导航,素材行保留分析状态并可直接新建关联作品;Lingui 1987/1987、lint、typecheck、4283 项测试及 1100×700 浏览器检查通过;无偏离。
- 2026-08-20, IA-14:删除退役页面包装器、录制索引和独立 montage/editor 列表,收口真实页面绑定与旧术语测试,逐条核对能力保留清单并完成 17 页浏览器审计;Lingui 1955/1955、lint、typecheck、4238 项测试通过;偏离见 PRD「落地记录与偏离」。
- 2026-08-20, IA-15:完成七个一级入口截图审计与页面几何收口,统一 56px 标题栏、46px 次级控制带、380px 上下文面板,删除成品页重复标题并完成 1280×720/1100×700 对比复核;lint、typecheck、355 个测试文件/4246 项测试通过;无偏离(D1 既有视觉体系保持不变)。
- 2026-08-20, IA-16:左上角品牌位替换为剪辑/分析模式按钮,模式持久化并按路由自动归位,两套一级导航共享资料与设置;定向 176 项测试、lint、typecheck 通过并已 push;无偏离。
- 2026-08-20, IA-17:新增首次打开模式选择引导,明确共享数据、模式职责与后续切换入口,选择/保持当前模式后完成一次性引导;定向 47 项测试、lint、typecheck 通过并已 push;无偏离。
- 2026-08-20, IA-18:参考 CS Demo Manager 路由与能力面补齐逐项映射,新增分析导航/路由/Ctrl K/九视图跨层保留契约;lint、typecheck、357 个测试文件/4265 项测试与双模式截图回归通过;明确 Ban tracking、全局队伍目录、语音导出为当前契约外能力,未创建空入口。
- 2026-08-20, IA-16 follow-up:用 shadcn/Radix `DropdownMenuRadioGroup` 替换通用溢出菜单,补齐剪辑→分析→剪辑往返回归;通过 WebView2 CDP + agent-browser 在真实 Tauri Desktop 验证 URL、模式状态和导航同步;Lingui 1967/1967、lint、typecheck、357 个测试文件/4265 项测试通过。
- 2026-08-20, IA-16 follow-up 2:修复侧栏 footer 横向分栏导致设置激活面缺角的问题,将折叠按钮改为叠加控件;恢复剪辑模式「Agent 创作」快捷入口并直达作品工作区唯一 Agent 面板;通过 WebView2 CDP + agent-browser 复现与验证。

---

## 阶段 7 —— 一句话 Demo 成片

> 本阶段落实 PRD「2026-08-20 追加目标：一句话成片」。原 D3 的后端零改动边界不适用于 IA-21 至 IA-25；需要的 Rust 契约必须真实落地，不能用页面状态或占位数据替代。

### IA-19 Demo + 首句启动

- [x] **状态**
- 建什么：Demo 行动作直达关联作品的 Agent 剪辑单；作品把主 Demo、作品 id、剪辑单 id/修订注入 Agent；无对话时输入第一句自动创建、命名并立即发送，不再要求先点「新建对话」。
- 验收标准：
  - [x] Demo 行主创作动作直接落到 `step=shotlist`
  - [x] 首句发送只需一次用户动作，并创建一条可在历史中继续的对话
  - [x] `AgentChatInput` 带正确 `demoId`、`workspaceContext.projectId`、plan id/revision
  - [x] 创建成功但模型失败时，问题与重试入口仍保留
  - [x] 新作品不再同时显示「没有对话」与「没有剪辑单」两个竞争空态

### IA-20 统一准备检查与创作层级

- [x] **状态**
- 建什么：把 Demo/分析/模型/录制环境收敛成一份 readiness 模型与启动画布；模型缺失提供直达设置项和测试连接；统一「剪辑单」术语；项目内「制作方式」与对话结果视图分层呈现。
- 验收标准：
  - [x] 准备检查的每个阻塞都有当前页解释和直接修复动作
  - [x] 对话结果视图只在产生对应内容后出现
  - [x] Agent 辅助/快速剪辑/多轨精剪明确标为制作方式，跨模式转换边界可读
  - [x] 录制交接只有一个主动作，文案随状态从生成剪辑单推进到确认剪辑单并录制

### IA-21 持久修改决策与回答生命周期

- [x] **状态**
- 建什么：domain/application/storage 增加提议修改决策和回答 turn；状态穷举 `pending/streaming/completed/cancelled/failed`，记录重试来源；接受/拒绝通过服务端写入并可恢复。
- 验收标准：
  - [x] 刷新和重启后接受/拒绝不丢
  - [x] 取消与失败留下可读、可重试的 turn，不冒充完成回答
  - [x] 同一 turn 的并发完成/取消写入有条件更新保护
  - [x] 旧会话文档兼容解码并有迁移测试

### IA-22 服务端提议绑定与成本元数据

- [x] **状态**
- 建什么：stream 请求携带 plan 基线，服务端捕获 proposal 时写 `plan_id/based_on_revision`；每个 assistant turn 写 provider/model/input/output token 与估算成本。
- 验收标准：
  - [x] 客户端不再为 proposal 补 plan/revision
  - [x] 过期 proposal 在服务端应用入口被拒绝
  - [x] 对话详情能按轮查看模型、token 与成本；缺失 usage 时明确显示未知

### IA-23 修改预览

- [x] **状态**
- 建什么：为可应用修改解析受影响片段与时间窗；优先播放已有 Take/录制文件，否则调用只读游戏预览；设置项控制接受前是否先预览。
- 验收标准：
  - [x] 每条可应用修改都有预览动作和明确来源
  - [x] 预览失败不改变剪辑单或修改决策
  - [x] `preview_before_apply` 开启时接受先进入预览确认，关闭时直接应用且仍可撤销

### IA-24 Take / Composition

- [x] **状态**
- 建什么：新增 Take 与 Composition domain/storage/routes；录制完成登记 Take；作品可为每个片段选择 Take，Composition 保持引用完整；按设置清理未引用旧 Take。
- 验收标准：
  - [x] 同一片段可列出、播放、比较多条 Take
  - [x] Composition 精确绑定每个片段的所选 Take 和顺序
  - [x] 被 Composition 引用的 Take 永不因 `take_limit` 自动删除
  - [x] 导出只读取已确认 Composition，不从目录猜文件

### IA-25 确认后自动录制、组合与导出

- [x] **状态**
- 建什么：把剪辑单确认后的录制、Take 登记、Composition 更新和最终导出编排成可恢复流程；作品步骤显示当前阶段、失败恢复和最终文件。
- 验收标准：
  - [x] 用户只确认一次，之后默认自动推进到可播放文件
  - [x] 任一片段失败可单独重试并继续同一 Composition
  - [x] 关闭应用后可从持久任务状态继续，不重复已完成录制
  - [x] 成品文件可播放、可定位，并反向关联作品与 Composition

### IA-26 可访问性、跨模式与真实闭环收口

- [x] **状态**
- 建什么：补齐 live region、焦点推进、键盘路径和跨模式转换说明/动作；用真实 Tauri Desktop 跑完一句话到视频并全量回归。
- 验收标准：
  - [x] 流式完成、修改应用、录制/导出状态有礼貌播报
  - [x] 完成动作后焦点落到下一项待处理内容
  - [x] 三种制作方式的可转换方向、复制语义和不可逆边界有测试
  - [x] Tauri WebView2 CDP + agent-browser 真实闭环截图与日志留证
  - [x] `pnpm lint && pnpm typecheck && pnpm test`、Rust 测试、i18n 目录全部通过

### 阶段 7 执行日志

- 2026-08-20, IA-19:Demo 行直达 Agent 剪辑单，首句自动创建并命名对话；请求显式携带 Demo、作品、剪辑单 id/修订，Desktop 在模型调用前读取并校验真实剪辑单；初始双空态合并为单一启动画布。lint、typecheck、357 个测试文件/4270 项前端测试与 Desktop 44 项测试通过；真实 Tauri CDP 验证模型未配置时用户原句和重试入口保留，证据见 `docs/superpowers/evidence/2026-08-20-ia-19/`；无偏离。
- 2026-08-20, IA-20:新增 Demo/分析/模型/录制四项统一准备检查与直接修复深链；模型设置由只读状态升级为可编辑、保存和真实连接测试；制作方式与结果视图分层，初始态隐藏修改视图与建议芯片，主动作收敛为生成剪辑单/确认剪辑单并录制。lint、typecheck、358 个测试文件/4275 项测试通过；Tauri CDP + agent-browser 在 2160px 与 1100×700 复核，证据见 `docs/superpowers/evidence/2026-08-20-ia-20/`；无偏离。
- 2026-08-20, IA-21:接受/拒绝写回会话 proposal 文档并从服务端恢复；assistant turn 在流式前持久建立，以条件状态更新覆盖 streaming/completed/cancelled/failed 和 retry_of；模型历史改由已完成的持久会话条目提供，Desktop thread 不再是第二份对话真值。未改 SQLite schema 指纹，旧 proposal/assistant 文档通过 defaulted optional fields 兼容解码；lint、358 个测试文件/4279 项前端测试及 domain/storage/application/desktop 四 crate 全量测试通过；无偏离。
- 2026-08-20, IA-22:Desktop 在已校验的请求上下文中为每条 proposal 写入 plan id/基准修订，客户端补章逻辑退役；计划写入口同时校验 proposal base，不能用新的 expected revision 重放旧 proposal；Rig 聚合的真实 usage 与 provider/model 按 turn 持久化，缺 usage 或权威定价时明确显示未知而非 0。lint、358 个测试文件/4280 项前端测试及 agent/domain/storage/application/desktop 五 crate 全量测试通过；locale 覆盖单页预算由 5 秒调到 10 秒以适应完整并发套件，断言未削弱；无偏离。
- 2026-08-20, IA-23:每条可应用修改在对话与剪辑单两处都提供预览；优先按录制元数据中的真实 `request_id` 播放对应 Take，没有 Take 时才编译镜头并二次确认启动 CS2 只读预览；`preview_before_apply` 开启时接受只打开预览，二次确认后才走原有条件写入。lint、类型检查及 4 个相关测试文件/65 项测试通过；完整前端套件首次运行因 Vitest 进程未退出手动中止，留待 IA-26 统一全量回归；无产品逻辑偏离。
- 2026-08-20, IA-24:新增 Take、Composition 与录制运行绑定的 domain/storage/API 契约，旧数据库按已知指纹原位迁移；录制文件以镜头请求 id 登记为多条 Take，Composition 用关系表精确固定镜头、Take 与顺序；清理只删除超限且未被引用的 Take；候选片段视图改为真实视频比较与持久选择，覆盖已确认 Composition 需二次确认；导出只能从完整、已确认 Composition 构建 montage。domain 282、storage 162（1 项环境测试忽略）、application 326 项测试全部通过；无偏离。
- 2026-08-20, IA-25:Agent 录制计划持久绑定作品与 Composition，录制完成自动登记 Take、补齐并确认 Composition，在检测到编码器后自动导出；失败/取消的重试计划继承同一绑定且只执行已证明的未完成后缀，重启后工作流查询会从持久任务重新协调，不重复完成片段；任务详情新增录制→Take→组合导出→成品的礼貌播报与失败恢复，成品反向保存 export job/path 并可定位。lint、类型检查、360 个前端测试文件/4286 项测试及三 crate 全量测试通过；无偏离。
- 2026-08-20, IA-26（待真实素材收尾）:对话 log、修改决策和 Agent 成片工作流补齐 polite live region；接受/拒绝后焦点按 DOM 顺序落到下一条待处理修改。制作方式采用单向副本图 `Agent → 快速剪辑 → 多轨精剪`：Agent 打开已物化的 Composition 快速副本，快速剪辑可原子复制片段顺序/裁切与媒体资产到新多轨工程，所有反向路径明确展示无法恢复的 Demo 证据、包装/转场或多轨信息，源工程永不被修改且后续不互相同步。真实 Tauri WebView2 通过 CDP 9222 + agent-browser 在 1440×900 验证数据库迁移、启动画布、模型设置深链和转换边界，证据见 `docs/superpowers/evidence/2026-08-20-ia-26/`；当前机器没有已导入 Demo 和模型凭据，因此未伪造数据完成真实 CS2 成片，最终硬件/素材闭环保持未勾选。lint、typecheck、361 个前端测试文件/4291 项测试、application 327、storage 162（1 项环境测试忽略）、desktop 46（1 项需 sidecar 环境测试忽略）全部通过。
- 2026-08-20, 完成审计补强:逐条追踪成功标准时发现 `video_render` 提议只进入会话、没有写入首版剪辑单，以及未分析 Demo 仍在发送前阻塞。现由 Desktop 在空占位剪辑单上条件物化唯一 `video_render`，同步建立不可变 Agent baseline 与 Agent 作者通知；前端完成流后刷新服务端剪辑单。Demo 为 discovered/indexing/analyzing/failed 时，首句会自动发起或加入分析并等待 ready，再调用模型；仅物理文件 missing 继续阻塞。新增 Desktop 纵向测试 `one_sentence_materializes_a_plan_and_reaches_a_persisted_final_video`，在隔离持久库中贯穿已分析 Demo + 一句话 → 剪辑单 → 录制 → Take → Composition → 导出 → 重启恢复；真实用户 Demo/CS2 的 CDP 硬件留证仍待外部输入。
- 2026-08-20, 完成审计第二轮:空剪辑单的首句显式使用视频规划模式；已有剪辑单的后续自然语言修改新增真实 `draft_agent_plan_changes` 工具，只接受当前 plan 中的 shot id，支持前端可直接应用的 shorten/delete，before/after 与 delta 由本地工具核算，Desktop 再校验 plan/revision 和 payload。完整回归为前端 361 文件/4294 项、agent 18、domain 283、storage 163（1 项环境测试忽略）、desktop 48（1 项 sidecar 环境测试忽略）全部通过；逐项证据见 `docs/superpowers/evidence/2026-08-20-ia-26/completion-audit.md`。
- 2026-08-20, IA-26 真实数据继续验收:从已校验 Major 归档恢复 M1，真实导入/分析得到 de_mirage、21 回合、10 选手与 457 条高光；OpenCode 的 Kimi K3 凭据仅注入 Desktop 进程，真实流式 turn 以 105246 tokens 调用证据/镜头工具并物化 FalleN R20 四杀方案。修复运行时模型状态误判、空方案错误工具、8 轮上限、失败工具留痕、StrictMode 录制租约回调丢失、自动发现 CS2 与 HLAE profile 漂移，以及 1440×900 录制启动态的 33px 竖字列布局。真实录制仍在当前 CS2 build 24701871 + managed HLAE 2.191.1 上稳定失败为 `HLAE_BRIDGE_TIMEOUT`；DLL 已加载但 bridge 无连接，临时 cfg/netcon 探针均已撤除，因此最终 MP4 验收保持未勾选。
- 2026-08-20, IA-26 录制预览几何收口:导播地图按单个镜头的相机关键点、朝向长度与安全边距计算局部裁剪，不再为一个镜头展示整张地图；基图与 SVG 共用同一聚焦投影。每帧只绘制当前播放位置的一组朝向/FOV，去掉起终点文字叠加；已有方案时忽略残留 pending 状态，镜头列表不再回退成骨架。最大化 Tauri WebView2 `2560×1392` 验证聚焦区域占地图约 17.9%、4 个路径关键点、1 组朝向/FOV、0 个起终点标签、无文档横向溢出，预检收束为 1024px/4 列；证据见 `target/real-data/agent-k3-real/camera-preview-focused-after.png`。
- 2026-08-20, IA-26 录制预览结构重做:进一步用 CDP 量化发现旧地图内容高 825px、承载父区仅 550px，收缩父区仍按错误高度排布后续控件，形成真实覆盖。导播区现改为正常流内的「地图舞台 + 镜头遥测」结构：最大化时左右分栏，时间线、播放、说明和导播依据统一进入 352px 侧栏；紧凑宽度自动上下排列，长证据允许断行。真实 Tauri 在 `2560×1392` 测得地图/遥测、地图/高度、地图/依据相交面积均为 0，地图完整包含于舞台，页面无横向溢出；`1440×900` 自动变为单列且无溢出。证据见 `target/real-data/agent-k3-real/camera-preview-redesigned.png`、`camera-preview-redesigned-bottom.png` 与 `camera-preview-redesigned-1440x900.png`。
- 2026-08-20, IA-26 全产品验证与恢复链路收口:使用真实 Tauri WebView2 CDP + agent-browser 完成 29 个页面/一级子视图在 `1100×700`、`1440×900`、`2560×1392` 的 87 份接受截图，修复 Steam 配置恢复深链、分析英文/裸 SteamID、457 行高光全量挂载、恢复中心 CS2 路径漂移和缺失 HLAE 无准备动作。Web 361 文件/4308 项、lint/i18n/typecheck/build、application 329、HLAE 69（1 环境项忽略）、runtime 247（5 环境项忽略）全部通过。稳定 2.191.1 与官方 2.192.1 prerelease 在当前 CS2 build 24701871 上均未执行生成的 bridge，真实 Take/Composition/MP4 仍受 `HLAE_BRIDGE_TIMEOUT` 外部兼容性阻塞，故 IA-26 与硬件闭环保持未勾选；证据见 `docs/superpowers/evidence/2026-08-20-full-product-verification/`。
- 2026-08-21, IA-26 真实成片闭环 PASS:用最小 HLAE 探针证明原 `HLAE_BRIDGE_TIMEOUT` 是 Steam 客户端未运行导致 CS2 在执行 `mirv_*` 前 fatal，而非 HLAE/当前 CS2 版本不兼容；Runtime 现可自动静默启动并等待 Steam 就绪。R20 镜头的留白跨过 R21 full-packet 边界，现按权威回合区间裁切，bridge 对瞬时播放缺失使用 5 秒有界 grace，并保留认证失败原因。真实录制作业 `2e3dee81-2bb0-4589-aafa-d4ed1e5ff88e` 完成，登记 Take `2e9bc7c7-f640-4cee-8ba4-be00137ac021`、确认 Composition `a86bf42c-f363-4aef-aedd-2c8f35b43a83`并完成导出 `2b8732b3-f3c1-49ee-acfe-02764e0e8d4e`；最终 MP4 为 1920×1080/60fps/H.264/AAC/8.55s/9,280,863 bytes，任务详情与作品导出步均可直达成品。Web 361 文件/4310 项、lint/i18n/typecheck/build、application 329、storage 163（1 真实数据项忽略）、HLAE 69（1 官方压缩包项忽略）、runtime 251（5 环境项忽略）通过；证据见 `docs/superpowers/evidence/2026-08-20-full-product-verification/`。
- 2026-08-21, IA-26 成片内容复验 PASS:逐帧复查发现首个 MP4 虽技术闭环，但无碰撞几何校验的静态外置机位被墙体遮挡，且 DemoUI 控制条被录入。Agent 现在只在碰撞几何已验证时保留电影机位，否则自动降级为经 SteamID/第一人称模式校验的选手 POV；DemoUI/HUD 安全命令提前到 `playdemo` 前。真实 POV 任务 `b7f87379-aa84-44b5-81e0-d82bc8be2dc8` 完成并登记 Take `77aa7f42-5eee-408a-b2fb-e4953f6f0e91`，显式替换已导出 Composition 后重新导出 `ccb4dce8-8869-4e74-bc02-e2595eaec327`。最终 MP4 为 1920×1080/60fps/H.264/AAC/8.583s/9,239,427 bytes；全片抽帧无 DemoUI，关键段连续出现敌人、交火与倒地。证据见 `docs/superpowers/evidence/2026-08-21-capture-content-fix/`。
